import { AvailableNowPipe } from './available-now.pipe';

describe('AvailableNowPipe', () => {
  it('create an instance', () => {
    const pipe = new AvailableNowPipe();
    expect(pipe).toBeTruthy();
  });
});
